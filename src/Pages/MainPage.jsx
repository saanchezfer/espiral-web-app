import React from "react";
import AppFrame from "../Components/AppFrame/AppFrame";
import Typography from "@mui/material/Typography";

const MainPage = () => {
  return (
    <AppFrame>
      <Typography>Primer test de la app</Typography>
    </AppFrame>
  );
};

export default MainPage;
